[project]
name = "lenya-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.9",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
